import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function CreateOutreachPage() {
  return (
    <PlaceholderScreen
      description="A future workspace for drafting cold emails and LinkedIn messages with approved Signal knowledge."
      eyebrow="Sales workflow"
      intent="Phase B only establishes the route and layout. AI generation, approved-claim insertion, and message composition controls will be added later."
      title="Create Outreach"
    />
  );
}
