import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function ReplyToProspectPage() {
  return (
    <PlaceholderScreen
      description="A future workspace for pasting a prospect response and preparing a professional reply."
      eyebrow="Sales workflow"
      intent="This placeholder does not analyze messages or generate responses yet. The foundation keeps the screen ready without adding AI behavior."
      title="Reply to Prospect"
    />
  );
}
