import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function BuildSequencePage() {
  return (
    <PlaceholderScreen
      description="A future workspace for planning non-repetitive multistep outbound sequences."
      eyebrow="Sales workflow"
      intent="Sequence-building logic, templates, and validation will arrive in a later phase. No automated sending or LinkedIn automation is included."
      title="Build Sequence"
    />
  );
}
